name: deploy

on:
  push:
    branches: [main]
    tags: [v*]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v3

      - uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: meta
        uses: docker/metadata-action@v4
        with:
          images: ghcr.io/${{ github.repository }}
          labels: |
            org.opencontainers.image.licenses=MIT OR Apache-2.0

      - name: Push Payout Script Container
        uses: docker/build-push-action@v3
        with:
          context: .
          file: docker/Dockerfile.payout
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - id: meta_sync
        uses: docker/metadata-action@v4
        with:
          images: ghcr.io/${{ github.repository }}-data-sync
          labels: |
            org.opencontainers.image.licenses=MIT OR Apache-2.0

      - name: Push Dune Sync Container
        uses: docker/build-push-action@v3
        with:
          context: .
          file: docker/Dockerfile.sync
          push: true
          tags: ${{ steps.meta_sync.outputs.tags }}
          labels: ${{ steps.meta_sync.outputs.labels }}
